---
name: remote-ssh
description: Execute commands on configured remote servers over SSH for deployment, monitoring and log inspection.
---

# Remote SSH Skill

Use `ssh_exec` when the user asks to check remote server status, deploy application builds, or inspect remote server logs.
